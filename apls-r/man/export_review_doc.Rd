% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review-docs.R
\name{export_review_doc}
\alias{export_review_doc}
\title{Export a website page as a Word review copy}
\usage{
export_review_doc(
  page,
  project_root = ".",
  site_dir = "docs",
  output_dir = "review-exports",
  site_url = "https://ap-ls.org",
  title = NULL,
  date = Sys.Date(),
  include_code = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{page}{Path to a source \code{.qmd} page, relative to \code{project_root} or as
an absolute path.}

\item{project_root}{Path to the Quarto website root. Defaults to the current
working directory.}

\item{site_dir}{Rendered website directory, relative to \code{project_root}.
Defaults to \code{"docs"}.}

\item{output_dir}{Directory for generated review copies, relative to
\code{project_root} or as an absolute path. Defaults to \code{"review-exports"}.}

\item{site_url}{Public website URL used to construct a link to the live
page. Use \code{NULL} to omit the link.}

\item{title}{Optional review-document title. By default, the title is read
from the rendered page.}

\item{date}{Export date included in the document and filename.}

\item{include_code}{Whether to retain displayed source-code blocks.
Defaults to \code{FALSE}.}

\item{overwrite}{Whether to replace an existing export with the same name.}
}
\value{
The absolute path to the generated \code{.docx} file.
}
\description{
Builds a Word review copy from a page in the rendered AP-LS website. The
copy includes the source path, live page URL, export date, and a short note
for reviewers. The source \code{.qmd} file is read but never changed.
}
\details{
The website must be rendered first. Reading the rendered HTML keeps
computed content in the review copy and avoids rendering one website page
in isolation.
}
\examples{
\dontrun{
export_review_doc("awards/grants/impactgrant.qmd")
}
}
