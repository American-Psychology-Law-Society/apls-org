% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{awards_table}
\alias{awards_table}
\title{Year-grouped award recipients table}
\usage{
awards_table(
  data,
  name = "name",
  detail = NULL,
  detail_label = "",
  subtitle = NULL,
  group = "year",
  page_size = 60
)
}
\arguments{
\item{data}{A data frame with at least a year column and a recipient-name
column. Extra columns are hidden unless named in \code{detail} or \code{subtitle}.}

\item{name}{Name of the recipient column. Default \code{"name"}.}

\item{detail}{Optional name of a column shown as a muted second column
(e.g. award type, affiliation, advisor). Default \code{NULL}.}

\item{detail_label}{Header shown above the \code{detail} column. Default \code{""}
(no header, the historical behaviour).}

\item{subtitle}{Optional name of a column rendered in muted italics under
the recipient's name (e.g. "Small grant"). Default \code{NULL}.}

\item{group}{Name of the year column to sort and label by. Default \code{"year"}.}

\item{page_size}{Rows per page. Default \code{60}.}
}
\value{
An \code{\link[htmltools:div]{htmltools::div()}} wrapping a \code{\link[reactable:reactable]{reactable::reactable()}} widget.
}
\description{
Renders a two- (or three-) column table of award recipients by year, in the
shared house style. Used for awards with one or more named recipients per
year (e.g. Saleem Shah, Distinguished Contributions, Teaching, Undergraduate).
}
\examples{
df <- data.frame(year = c(2025, 2024), name = c("A. Smith", "B. Jones"))
awards_table(df)
}
