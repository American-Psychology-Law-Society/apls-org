% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{awards_timeline}
\alias{awards_timeline}
\title{Emit a quarto-timeline from award data}
\usage{
awards_timeline(
  data,
  label = "year",
  content = "content",
  classes = c("timeline", "vertical-alt", "tl-card"),
  style = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{label}{Name of the column used as the marker label (year).
Default \code{"year"}.}

\item{content}{Name of a column holding the markdown shown for each entry.
Default \code{"content"}.}

\item{classes}{Character vector of timeline classes. Default
\code{c("timeline", "vertical-alt", "tl-card")}.}

\item{style}{Optional inline CSS (e.g. \verb{--tl-color-*} overrides) applied to
the outer \code{.timeline} div. Default \code{NULL}.}
}
\value{
Invisibly \code{NULL}; called for the side effect of \code{cat()}-ing markup.
}
\description{
Prints Pandoc fenced-div markup for the
\href{https://emilhvitfeldt.github.io/quarto-timeline/}{quarto-timeline}
extension. MUST be called from a chunk with \verb{#| output: asis}, and the page
must enable the filter (\verb{quarto add EmilHvitfeldt/quarto-timeline} and
\verb{filters: [timeline]} in the YAML). Rows sharing a \code{label} (year) are grouped
under one marker by the extension.
}
\examples{
df <- data.frame(year = 2026, content = "**A. Smith**")
awards_timeline(df)
}
