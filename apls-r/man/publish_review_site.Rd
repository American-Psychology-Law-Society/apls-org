% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review-site.R
\name{publish_review_site}
\alias{publish_review_site}
\title{Publish a complete website review snapshot to Google Drive}
\usage{
publish_review_site(
  project_root = ".",
  drive_path = "Website Reviews/Full Site",
  shared_drive = NULL,
  site_dir = "docs",
  site_url = "https://ap-ls.org",
  output_dir = "review-exports/full-site",
  snapshot_id = paste("Site Snapshot", Sys.Date()),
  index_name = "AP-LS Website Review Index",
  date = Sys.Date(),
  render = TRUE,
  dry_run = TRUE,
  resume = TRUE,
  include_404 = FALSE
)
}
\arguments{
\item{project_root}{Path to the Quarto website root.}

\item{drive_path}{Folder path in My Drive or a Google Shared Drive. Missing
folders are created.}

\item{shared_drive}{Optional exact Shared Drive name.}

\item{site_dir}{Rendered website directory.}

\item{site_url}{Public website URL.}

\item{output_dir}{Local directory for snapshots and manifests.}

\item{snapshot_id}{Folder name for this snapshot.}

\item{index_name}{Name of the Google Sheet that maps website URLs to the
current review documents. The same Sheet is updated on later runs so Form
automation can keep using one file ID.}

\item{date}{Date used in review-document filenames.}

\item{render}{Whether to render the complete Quarto project first.}

\item{dry_run}{Whether to return the planned manifest without writing or
uploading anything.}

\item{resume}{Whether to reuse a matching manifest from an interrupted run.}

\item{include_404}{Whether to include the website's 404 page.}
}
\value{
Invisibly, the snapshot manifest as a data frame.
}
\description{
Exports every rendered website page as a Word review copy and uploads it as
a native Google Doc. A local manifest is updated after every page so an
interrupted run can resume without re-uploading completed pages.
}
\examples{
\dontrun{
publish_review_site(
  drive_path = "Website Reviews/Full Site",
  shared_drive = "ap-ls.org",
  dry_run = TRUE
)
}
}
